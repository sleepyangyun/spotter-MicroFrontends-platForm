import JSONBigint from 'json-bigint';

export const JSONBig2String = JSONBigint({ storeAsString: true });
export const DATE_TIME_FORMAT = 'YYYY-MM-DD HH:mm:ss'
export enum Lang {
    EN = 'en',
    ZH = 'zh'
}